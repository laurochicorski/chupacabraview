export default class Cfop {
    
    constructor(id, descricao, dtCriacao, dtAlteracao) {
        this.id = id;
        this.descricao = descricao;
        this.dtCriacao = dtCriacao;
        this.dtAlteracao = dtAlteracao;
    }
}